import 'package:flutter/material.dart';

class IbaiScreen extends StatelessWidget {
  const IbaiScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
